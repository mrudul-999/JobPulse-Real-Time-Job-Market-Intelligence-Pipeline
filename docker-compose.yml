services:
  postgres:
    image: postgres:15
    container_name: job_pulse_postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: job_pulse
    ports:
      - "54302:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: